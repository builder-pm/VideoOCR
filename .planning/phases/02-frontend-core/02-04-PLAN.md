---
phase: 02-frontend-core
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: [index.html]
autonomous: true
requirements: [FRONT-10, FRONT-11, FRONT-12, FRONT-13, FRONT-14, FRONT-15, FRONT-16, FRONT-17, FRONT-18]

must_haves:
  truths:
    - "User can adjust FPS via a slider with labeled stops"
    - "Estimated frame count and processing time update in real-time"
    - "Warning badge appears if frame count > 5000"
    - "Advanced options panel is collapsible and contains all required controls"
  artifacts:
    - path: "index.html"
      provides: "Configuration panel and estimation logic"
---

<objective>
Implement the extraction settings panel and real-time estimation logic to help users configure the OCR pipeline effectively.

Purpose: Provide fine-grained control over the extraction process.
Output: Interactive configuration sidebar with real-time feedback.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-core/02-UI-SPEC.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FPS Slider & Controls</name>
  <files>index.html</files>
  <action>
    Implement the primary controls in the Sidebar:
    1. FPS Slider (0.5 to 30) with visual stops/labels at [0.5, 1, 2, 5, 10, 15, 30].
    2. Use a datalist or custom labels for the stops.
    3. Update App.config.fps on change.
  </action>
  <verify>
    - Slider allows selecting values between 0.5 and 30.
    - Labels for stops are visible.
  </verify>
  <done>FPS selection is functional.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Real-time Estimation</name>
  <files>index.html</files>
  <action>
    Create a dynamic stats block:
    1. Calculate Estimated Frames: (end_time - start_time) * fps.
    2. Calculate Estimated Time: Assume ~1 second per frame (heuristic).
    3. Update display in real-time as range or FPS changes.
    4. Implement the Warning Badge (Amber color) if frames > 5000.
  </action>
  <verify>
    - Changing FPS or Range updates the 'Estimated Frames' text.
    - Warning badge appears when range * FPS > 5000.
  </verify>
  <done>User receives immediate feedback on their settings.</done>
</task>

<task type="auto">
  <name>Task 3: Build Advanced Options Panel</name>
  <files>index.html</files>
  <action>
    Implement a collapsible 'Advanced Extraction Settings' section in the sidebar:
    1. Blur Threshold Slider (0-300, default 100).
    2. OCR Engine Toggle (Segmented control or radio buttons): PaddleOCR vs Tesseract.
    3. Deduplication Toggle (Switch/Checkbox, default ON).
    4. Language Selector (Dropdown): English, Hindi, English+Hindi.
    5. Ensure the panel defaults to closed.
  </action>
  <verify>
    - Panel expands/collapses smoothly.
    - All 4 controls are present and update App.config.
  </verify>
  <done>Advanced settings are available but non-intrusive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (Client-side configuration)
</threat_model>

<verification>
1. Upload a video.
2. Adjust FPS to 5. Verify estimated frames update.
3. Select "Full Duration" for a long video. Verify warning badge appears if > 5000 frames.
4. Expand Advanced Settings and toggle OCR engine.
</verification>

<success_criteria>
- Configuration sidebar is fully functional.
- Estimation logic is accurate based on inputs.
- Advanced settings are correctly mapped to state.
</success_criteria>

<output>
After completion, create .planning/phases/02-frontend-core/02-04-SUMMARY.md
</output>
