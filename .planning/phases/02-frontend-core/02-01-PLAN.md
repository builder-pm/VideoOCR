---
phase: 02-frontend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server.py, index.html]
autonomous: true
requirements: [FRONT-33, FRONT-34, FRONT-35, FRONT-36, FRONT-37]

must_haves:
  truths:
    - "Server serves index.html at the root URL"
    - "Page loads with dark-mode theme and Satoshi/JetBrains Mono fonts"
    - "UI has a 2-column layout (Sidebar for controls, Main for output)"
  artifacts:
    - path: "index.html"
      provides: "SPA structure and design foundation"
    - path: "server.py"
      provides: "Static file serving"
  key_links:
    - from: "index.html"
      to: "server.py"
      via: "API calls and static serving"
---

<objective>
Setup the SPA foundation for VideoOCR Studio, including the server-side static serving, the HTML shell with the required design system (colors, fonts), and the basic state management/API utility.

Purpose: Provide the container for all subsequent frontend features.
Output: Working index.html shell served by FastAPI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-frontend-core/02-UI-SPEC.md
@server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable static serving in server.py</name>
  <files>server.py</files>
  <action>
    Add a route to serve 'index.html' at the root path ('/').
    Ensure it uses FastAPI's FileResponse.
    Also, ensure the server is ready to handle static requests if needed (though we'll start with inline styles/scripts for portability per D-01).
  </action>
  <verify>
    <automated>curl -I http://localhost:8000/</automated>
  </verify>
  <done>Root URL returns 200 OK with text/html content type.</done>
</task>

<task type="auto">
  <name>Task 2: Create index.html with Design System</name>
  <files>index.html</files>
  <action>
    Create a single-file index.html.
    1. Include Satoshi (Sans) and JetBrains Mono fonts via CDN.
    2. Define CSS variables for the color palette from UI-SPEC (#0f0e0c, #141312, #01696f).
    3. Implement a 2-column layout:
       - Left Sidebar (fixed width ~320px, #141312 background).
       - Main Content Area (flex-grow, #0f0e0c background).
    4. Set default typography for H1, H2, Body, and Caption.
    5. Add a placeholder 'Upload' section in the Main area and 'Controls' in the Sidebar.
  </action>
  <verify>
    - Check for CSS variables: --bg-deep, --bg-panel, --accent.
    - Check for font-family: Satoshi, sans-serif.
    - Visual check of layout.
  </verify>
  <done>index.html exists with correct styles and layout structure.</done>
</task>

<task type="auto">
  <name>Task 3: Implement App State & API Helper</name>
  <files>index.html</files>
  <action>
    Add a &lt;script&gt; block to index.html with:
    1. An 'App' object to hold global state: { sessionId: null, metadata: null, config: {...}, status: 'idle' }.
    2. A 'UI' object for basic DOM manipulation helpers.
    3. An 'API' helper using fetch to communicate with server.py endpoints (/status, /upload, /process, etc.).
    4. On page load, call /status to check backend health and engine availability.
  </action>
  <verify>
    - Open browser console, 'App' and 'API' objects should be available.
    - API.getStatus() should return backend status.
  </verify>
  <done>JS foundation is ready for interactive features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser -> API | Untrusted JSON/File input crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | server.py (static serving) | mitigate | Use FileResponse for index.html, avoid directory traversal in static serving |
| T-02-02 | Information Disclosure | server.py | mitigate | Ensure cleanup endpoint is protected or session IDs are sufficiently random |
</threat_model>

<verification>
1. Run server: `python server.py`
2. Access http://localhost:8000/
3. Verify layout looks like a 2-column dark-mode app.
4. Verify console shows no errors and API check passed.
</verification>

<success_criteria>
- index.html is served at the root.
- Dark mode design system is applied.
- Basic JS state and API wiring is functional.
</success_criteria>

<output>
After completion, create .planning/phases/02-frontend-core/02-01-SUMMARY.md
</output>
