---
phase: 01-backend-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [requirements.txt, README.md, tests/test_server.py]
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - "Project dependencies are fully documented and installable"
    - "Setup and run instructions are clear for new users"
    - "Key backend functionality is verified through automated tests"
  artifacts:
    - path: "requirements.txt"
      provides: "Complete dependency list"
    - path: "README.md"
      provides: "Documentation and setup guide"
    - path: "tests/test_server.py"
      provides: "Pytest suite for backend endpoints"
  key_links:
    - from: "pytest"
      to: "FastAPI endpoints"
      via: "httpx TestClient"
---

<objective>
Finalize project documentation and implement a test suite to verify backend functionality.

Purpose: Ensure project maintainability and verify that core features (upload, metadata, processing, SSE) work as expected.
Output: Updated requirements.txt, new README.md, and a comprehensive test suite in tests/test_server.py.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md
@server.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Finalize documentation</name>
  <files>requirements.txt, README.md</files>
  <action>
    - Update `requirements.txt` to include `pytesseract`, `pytest`, `httpx`, and any other missing dependencies identified during development.
    - Create `README.md` with sections: Overview, Prerequisites (FFmpeg, Tesseract/PaddleOCR), Installation, Running the App, and Testing.
    - Include specific instructions for Windows/macOS/Linux if applicable (especially for FFmpeg and Tesseract).
  </action>
  <verify>
    <automated>ls README.md && grep "pip install" README.md</automated>
  </verify>
  <done>Project is documented and ready for external setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Create automated test suite</name>
  <files>tests/test_server.py</files>
  <behavior>
    - Test 1: POST /upload with a valid video (use a small dummy video file) returns 200 and metadata.
    - Test 2: POST /upload with an oversized file (>2GB mock) returns 413.
    - Test 3: POST /process starts a background task and returns 200.
    - Test 4: GET /progress (SSE) returns initial progress state.
    - Test 5: Mock OCR engine and verify extraction pipeline calls the mock correctly.
  </behavior>
  <action>
    - Use `pytest` and `httpx.AsyncClient` (or FastAPI's `TestClient`) to test the API endpoints.
    - Create a `conftest.py` if needed for shared fixtures.
    - Use `unittest.mock` to simulate FFmpeg and OCR engine outputs to ensure tests can run in environments without these tools.
  </action>
  <verify>
    <automated>pytest tests/test_server.py</automated>
  </verify>
  <done>Core backend functionality is verified by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A (Documentation and Tests)

## STRIDE Threat Register

N/A
</threat_model>

<verification>
- Run `pytest` and ensure all tests pass.
- Read through README.md to ensure clarity.
</verification>

<success_criteria>
- requirements.txt is accurate and complete.
- README.md provides clear setup instructions.
- Test suite covers at least 80% of backend endpoint logic.
</success_criteria>

<output>
After completion, create .planning/phases/01-backend-foundation/01-03-SUMMARY.md
</output>
