---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server.py]
autonomous: true
requirements: [BACK-01, BACK-02, BACK-03, BACK-04, BACK-05, BACK-06, BACK-07, BACK-13, BACK-14, BACK-15, BACK-16, BACK-17, BACK-18]
must_haves:
  truths:
    - "Multiple users can upload videos and receive unique session IDs"
    - "Progress is streamed via SSE with regular heartbeats"
    - "API correctly identifies and rejects oversized or too-long videos"
  artifacts:
    - path: "server.py"
      provides: "Session-aware FastAPI backend with SSE support"
  key_links:
    - from: "GET /progress"
      to: "SSE stream"
      via: "StreamingResponse"
---

<objective>
Refactor server.py to support multiple concurrent sessions and implement real-time progress streaming via Server-Sent Events (SSE).

Purpose: Move away from global state to enable reliable multi-user support and provide a better UX through streaming updates.
Output: Updated server.py with session management and SSE.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-backend-foundation-CONTEXT.md
@server.py
</context>

<tasks>

<task type="auto">
  <name>Refactor session management</name>
  <files>server.py</files>
  <action>
    - Replace the global `processing_state` dictionary with a more robust session-based storage.
    - Implement a `SessionManager` class or similar pattern to track active sessions using a dictionary of {session_id: state}.
    - Per D-03: Use UUIDs for session IDs and ensure all processed artifacts (uploaded video, extracted frames) are isolated in session-specific directories.
    - Update endpoints (`/upload`, `/process`, `/cancel`, `/results`, `/frames/{index}`) to look up state by session ID.
    - Ensure `/upload` returns the `session_id` to the client.
  </action>
  <verify>
    <automated>python -c "import server; assert 'processing_state' not in dir(server)"</automated>
  </verify>
  <done>Session management is isolated and supports multiple concurrent sessions.</done>
</task>

<task type="auto">
  <name>Convert /progress to SSE with heartbeats</name>
  <files>server.py</files>
  <action>
    - Per D-04: Refactor the `/progress` endpoint to use FastAPI's `StreamingResponse` for Server-Sent Events.
    - Implement a generator function that yields progress updates as `data: {json_payload}\n\n`.
    - Payload should include: `percent_done`, `current_frame`, `total_frames`, `skipped_blur`, `text_blocks` (delta or full), and `status` (processing/completed/failed).
    - Implement a heartbeat every 5-10 seconds (yielding `: heartbeat\n\n`) to keep the connection alive.
    - Send a final terminal event when processing is complete or has failed.
  </action>
  <verify>
    <automated>curl -N http://localhost:8000/progress?session_id=TEST_ID | head -n 5</automated>
  </verify>
  <done>/progress streams live updates via SSE with heartbeats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted file uploads and processing parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | /upload | mitigate | Enforce 2GB limit and 60min duration check as specified in requirements |
| T-01-02 | Information Disclosure | /frames/{index} | mitigate | Ensure session ID in path is validated against existing sessions to prevent cross-session access |
</threat_model>

<verification>
- Run server and verify multiple concurrent uploads work.
- Use curl to observe SSE stream and confirm heartbeats.
</verification>

<success_criteria>
- Backend handles multiple sessions without state bleed.
- SSE stream provides real-time updates and heartbeats.
</success_criteria>

<output>
After completion, create .planning/phases/01-backend-foundation/01-01-SUMMARY.md
</output>
