---
phase: 01-backend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [server.py]
autonomous: true
requirements: [BACK-08, BACK-09, BACK-10, BACK-11, BACK-12, BACK-19, BACK-20, BACK-21, BACK-22]
must_haves:
  truths:
    - "Backend detects missing FFmpeg or OCR engines and returns friendly error messages"
    - "Unsupported codecs are handled gracefully during metadata extraction"
    - "Processing pipeline handles subprocess errors without crashing the server"
  artifacts:
    - path: "server.py"
      provides: "Resilient processing pipeline with comprehensive error handling"
  key_links:
    - from: "subprocess.run"
      to: "error response"
      via: "try-except and returncode check"
---

<objective>
Enhance the backend processing pipeline with robust error handling for missing dependencies (FFmpeg, OCR engines) and unsupported video codecs.

Purpose: Provide user-friendly feedback when local environment prerequisites are not met, preventing silent failures or raw stack traces.
Output: Improved error handling in server.py.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
@server.py
</context>

<tasks>

<task type="auto">
  <name>Improve tool detection and error reporting</name>
  <files>server.py</files>
  <action>
    - Refactor `get_status` and utility functions to proactively check for FFmpeg/ffprobe existence on startup or before use.
    - Per D-05: Update `get_video_metadata` to catch cases where ffprobe is missing or returns non-zero exit codes (e.g., unsupported codec).
    - Update `extract_frames` to handle FFmpeg failures gracefully, capturing stderr and returning helpful messages to the user.
    - Enhance OCR engine detection: if a requested engine (PaddleOCR/Tesseract) is not available, return a 400 error with instructions on how to install it.
  </action>
  <verify>
    <automated>python -c "import subprocess; assert subprocess.run(['ffmpeg', '-version'], capture_output=True).returncode == 0"</automated>
  </verify>
  <done>Missing dependencies are detected and reported with friendly messages.</done>
</task>

<task type="auto">
  <name>Harden processing pipeline</name>
  <files>server.py</files>
  <action>
    - Wrap `process_video` loop in additional try-except blocks to ensure session state is updated to 'failed' if any individual step (blur detection, OCR) crashes.
    - Ensure temporary files are cleaned up or at least marked for cleanup even if processing fails.
    - Implement specific checks for common pitfalls like "No space left on device" or "Permission denied" during frame extraction.
  </action>
  <verify>
    <automated>python -c "from server import is_similar_text; assert is_similar_text('test', 'test')"</automated>
  </verify>
  <done>Pipeline is resilient to individual frame failures and environment issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> OS Subprocesses | Data passed to FFmpeg/OCR engines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | subprocess.run | mitigate | Use list-based arguments for subprocess.run (avoid shell=True) to prevent command injection |
| T-01-04 | Denial of Service | OCR processing | mitigate | Run OCR in ThreadPoolExecutor to prevent blocking the async event loop |
</threat_model>

<verification>
- Temporarily rename ffmpeg binary (if possible in test env) and verify API returns 400 with helpful message.
- Upload a non-video file renamed to .mp4 and verify error handling.
</verification>

<success_criteria>
- Helpful error messages for missing FFmpeg, Tesseract, or PaddleOCR.
- Server remains stable even when subprocesses fail.
</success_criteria>

<output>
After completion, create .planning/phases/01-backend-foundation/01-02-SUMMARY.md
</output>
